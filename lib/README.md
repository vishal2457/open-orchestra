# open-orch

CLI package for Open Orchestra.

Primary command:

```bash
npx open-orch init
```

This command creates `orchestra-settings.json` in the current directory and installs the core Open Orchestra skills from:

```bash
https://github.com/vishal2457/open-orchestra/tree/main/skills
```
